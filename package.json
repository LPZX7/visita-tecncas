{
  "name": "visitas-tecnicas",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "rm -rf frontend/node_modules && npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node backend/src/index.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run start --prefix frontend"
  }
}
